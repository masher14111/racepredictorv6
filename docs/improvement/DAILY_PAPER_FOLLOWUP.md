# Daily paper reliability follow-up
Authorized 2026-09-19: add DeepSeek from OpenRouter, then make daily paper operation reliable and ready for fresh evaluation, using the automatic coder outside the chat.

The original 18 stages retain their recorded outcomes. This is an appended six-stage work order,
not a rerun of that programme or a claim its open defects have been fixed.

| Step | Work | Runtime model | Thinking |
|---|---|---|---|
|19|Existing DeepSeek backend into capped daily shadow extraction|Claude Sonnet 5|High|
|20|Canonical race identity, fresh/started-race cutoffs, model provenance|Claude Sonnet 5|High|
|21|Paper stakes, real settlement engine, separate PASS observations|Claude Sonnet 5|High|
|22|Current declarations, honest fallback labels, source health|Claude Sonnet 5|High|
|23|Complete consistent shadow bundle and frozen prospective protocol|Claude Sonnet 5|High|
|24|Independent readiness audit, single daily launcher and bounded live smoke|Claude Opus 5|Extra high (xhigh)|

The model choices use the already-configured working CLI routes. Codex remains supported by the
same runner; its earlier subscription-limit override remains recorded in runner.json. No new paid
coding API account is required. Each stage runs in a fresh session and reads compact repository memory.
These model choices are practical assignments, not measured speed/quality rankings.

## Start, watch and resume
Use the existing outer START_AUTOMATIC_IMPROVEMENTS.cmd to start/resume at19.
WATCH_IMPROVEMENTS.cmd displays progress; STOP_AFTER_CURRENT_STEP.cmd requests a clean pause.
The terminal shows stage/model/effort, tool activity and 30-second elapsed-time updates.
Only one supervisor may own the shared checkout. No manual second coding worker in these files.
A terminal closing is not proof of completion: check logs/improvement-runner/status.json and STATE.md.
Account limits/errors stop with a saved checkpoint; starting again resumes the recorded stage.
The per-stage 3-hour and per-run 12-hour limits are unchanged; no completion-time guarantee is implied.

## Acceptance and boundaries
- Preserve the dirty tree, original stage dispositions and frozen historical evidence.
- Readiness blockers and repros: reports/improvement/17/READINESS.md (B1-B10).
- Model verdict stays NO-GO/PAPER-ONLY. Fresh SHADOW observation is distinct from the formal qualified-bet window.
- All old holdouts are observed. Freeze a new prospective cohort before collecting its predictions.
- DeepSeek means deepseek/deepseek-v4.1-flash via the existing OpenRouter adapter, for grounded text extraction.
- Reuse data/cache/llm/hosted_budget_ledger.json: US$5/1,200 requests TOTAL, including previous benchmark usage.
- No cap reset, top-up, secret logging, unsupported model substitution or probability generation by an LLM.
- Text-derived numerical model features remain disabled until separate genuine forward ablation evidence exists.
- Complete candidate targets in isolated shadow artifacts; preserve the served champion and rollback hashes.
- Source blocks require legitimate access; unavailable declarations remain an explicit excluded-data gap.
- Test offline on isolated stores, then at most a bounded live smoke per relevant stage; suppress external messages.
- Daily orchestration must be resumable, bounded and idempotent; it must not retrain the models every day.
- No scheduled recurring job, startup service or real-money operation is created by this coding programme.
- Future elapsed time, race results and validation thresholds remain pending until actually observed.

## Completion evidence
Each worker writes memory/improvement/stages/NN.md, reports/improvement/NN/ and updates its own ledger row.
The supervisor requires successful CLI exit, fresh evidence, unchanged protected work orders and valid memory.
Stage24 reports engineering readiness, source coverage, shadow collection, model verdict and forward evidence separately.
Required implementation defects stop the run. Missing live source access may remain a named external gap only
when safe exclusion and failure handling are tested; it must not be described as full data readiness.
The final operational instructions will be reports/improvement/24/OPERATIONS.md once stage 24 completes.

Original plan/state/handoff snapshots and stage-note hashes are in docs/archive/daily-paper-followup-20260919/.
