# Archived STATE observations — steps 06-09
Moved out of STATE.md on 2026-09-19 to keep the active file and the
generated CHAT_CONTEXT.md under their 200/199-line limits. Nothing is
truncated: the full stage evidence remains in memory/improvement/stages/.

- Step 06 (2nd attempt): verified D31-D34, then fixed a pre-session data-loss regression (betsp.parquet lost all 2026-07-25 positions), restored from frozen-baseline ground truth. Full suite 2,324 passed. Detail: stages/06.md, D35.
- Step 07: `execution/snapshots.py`'s point-in-time odds store predates this programme and was already correct (reverified). Fixed 2 live-only defects in `scripts/daily_paper_loop.py::_today_tickets`: (a) it reused the day-truncated ticket-id `now` as the gate's freshness instant, making every cached price read as negative-age "timestamp_in_future" (masked only because the model is NO-GO); (b) live pricing never went through the loop's own `SnapshotStore`. Both fixed + tested; corrected a ppwap-as-fillable overclaim in `backtest/holdout.py`/`data.py`. Bounded live capture confirmed idempotent append-only. Full suite 2,329 passed. Detail: stages/07.md, D36.
- Step 09 (independent audit, 2 attempts, **DONE — foundation APPROVED as engineering, not as profitability**): reproduced steps 02/03/04/06/07/08's load-bearing claims on real data and they hold (split/CV/calibration boundaries race-disjoint + chronologically admissible + row-order invariant on BOTH matrices; market identity, full-field, overround and market_rank invariants; D38 collapse loses 0 runner-events; snapshot append-only/point-in-time on the real db; settlement identity, ambiguity refusal, idempotency; ledger = 988 PASS / 0 wagers; no unsupported forward claim). Attempt 1 found and routed 2 blockers; attempt 2 repaired them and 3 more, all with tests: **F1** trailing windows cut priors positionally inside a date-only `race_date` (46-50% of counted same-day priors had not yet run) → now cut on the real off-time from `race_uid`, 0 later-running priors and 0 row-order sensitivity, and `build_inference_matrix` admits today's already-run races so serving matches training (D39); **F7** (new) the trainer/jockey day windows used a ns-per-day constant on a microsecond frame, so `window_days=14` meant 14,000 days and the 4 form-cycle features were dead (D41); **F3** `race_complexity` stripped from `PRICE_FREE_FEATURE_COLS` via `MARKET_DERIVED_FEATURE_COLS`, enforced by `audit/leakage.py` (D40); **F5** `runner_status` now consumed by `_is_non_runner`; **F6** `_canonical_id` rejects rendered-null names. Full suite 2,336 (attempt 1 start) → 2,343 → **2,365**. Detail: stages/09.md, reports/improvement/09/findings.md.
- Step 08: fixed `daily_paper_loop.py`'s historical-result settlement join, which indexed on `horse_id` alone globally (88% of horse_ids recur across dates on the real store) -- now a canonical (venue, off-time-to-the-minute, horse) key with day/venue-scoped fallbacks, ambiguous matches refused rather than guessed. Fixed `_paper_summary` (both `daily_paper_loop.py` and `paper_betting.py`) conflating PASS decisions with open wagers -- the "988 open tickets" figure was always 988 PASS records, 0 real candidates (reverified live). Found and deliberately deferred 2 further real defects: `execution.snapshots.race_uid` drops venue once an off-time parses (4/35,107 real collisions on the store, ~0.011%); the live loop's settlement never calls `execution.settlement.settle_ticket` (Rule4/dead-heat/each-way/non-runner), only the offline backtest does, even though `execution.race_facts`'s archive has current coverage. Full suite 2,336 passed. Detail: stages/08.md, D37.

## Archived blockers / superseded-artifact notes (steps 04/06/07/08/09)
- Step 07 (none block step 08, see stages/07.md): live odds capture is WIN-only; no explicit withdrawal column in odds_snapshots (absence + staleness is the signal); execution.safeguards' race-started buffer is not wired into the live daily paper loop; ppwap-as-fillable overclaim persists in models/train_lgbm.py, audit/report.py, scripts/calibration_audit.py.
- Step 08 (none block step 09, see stages/08.md): `execution.snapshots.race_uid` drops venue once an off-time parses (4/35,107 real collisions on the store, ~0.011%; CLV-only exposure, not win/lose) -- a fix needs a schema/data migration across odds_snapshots and paper_tickets, deliberately not attempted this stage. The live daily loop's settlement never calls `execution.settlement.settle_ticket` (Rule4/dead-heat/each-way/non-runner) -- only the offline backtest simulator does -- even though `execution.race_facts`'s Sporting Life archive has current coverage; wiring it in is a live-settlement architecture change, recommended as a named follow-up, not attempted this stage.
- `data/audit/04/training_pit_fixed.parquet` and `data/audit/06/training_refreshed.parquet` remain superseded (predate D38/D39/D41); step 10's `data/audit/10/training_rebuilt.parquet` is the current rebuilt matrix.
- The "independent" lines in reports/forward_validation_20260918.md were fitted with `race_complexity` in scope (D40) — superseded by step 10's genuinely price-free scorecard (reports/improvement/10/scorecard.json).
