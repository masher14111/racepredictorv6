# STATE.md observations for steps 10-16 — archived verbatim

Archived 2026-09-19 by step 17 to keep the active memory export under its 199-line cap
(AGENTS.md end-of-step rule 11). Text below is byte-for-byte what STATE.md carried; nothing
was reworded. Stage evidence: memory/improvement/stages/10.md .. 16.md. Durable reasons: D42-D48.

- Step 10 (2 attempts, **DONE**): attempt 1 built the pipeline (rebuilt matrix, frozen `run_manifest.json`, new `models/conditional_logit.py`/`models/split_utils.py`) but was killed mid-session waiting on an un-monitored smoke test; STATE correctly stayed PENDING. Attempt 2 verified attempt 1's artifacts, ran the smoke test to completion, then the real frozen-budget run (6 trials/5-fold CV): market-only, conditional-logit, CatBoost, grouped-softmax LightGBM, each independent + market-assisted, on one dev-OOS panel (618 races/5,561 runners, 2026-08-08..2026-08-27). **All 6 candidates NO-GO** (fail to beat market log loss 1.70403): independent branches -0.21..-0.23 log-loss gap (expected — price-free can't compete with the market), market-assisted branches -0.003..-0.015 (close but still short). This is the first genuinely price-free line (D40) and does not change the historical NO-GO verdict. Weighting ablation: unweighted/race-weighted sample weight beats the existing odds-inverse scheme by ~0.14 log loss in-fold on the conditional logit (disclosed, not adopted). Final holdout (2026-08-28..2026-09-17, 708 races) untouched — verified by code path, not just claim. Added reload/inference-parity checks (all 3 model families PASS) that attempt 1 had not reached. Full suite 2,378 passed (13 new tests). Detail: stages/10.md.

- Step 11 (**DONE**; sectionals **DEFERRED_DATA**, Candidate enabled: no): real measured timing DOES exist, in the raw Sporting Life archive already trusted for settlement — `winning_time` on 98.5% of race documents, race `distance` on 100%, per-ride `finish_distance` margins (proved INCREMENTAL by measurement: 373 sampled races decrease down the finishing order, impossible under a cumulative reading). **No sectional field exists anywhere in the archive** (recursive key scan) — that half is deferred, not faked. New `features/measured_timing.py` (ingestion contract: yards/seconds/lengths, non-runners excluded, non-finishers null-never-imputed, `available_from` = local off time, join key byte-identical to `race_facts`) extracted **31,592 usable races (96.23%)**, 296,009 runner rows, 2024-01-01..2026-09-18, joining **99.98%** of the matrix's 276,297 WIN rows. `features/measured_speed.py` fits point-in-time pars (8-level backoff, course/distance/surface/going/race-type, strictly-prior same-instant blocks) and derives 7 strictly-prior horse features via `_trailing_fast` (D26/D39). Invariance verified on REAL data: append-invariance 527,220 rows / **0 diffs**, row-order 551,815 rows / **0 diffs**. Ablation on step 10's frozen windows and identical 618-race panel, all 3 baselines reproducing step 10 exactly: price-free conditional logit **1.92141 → 1.91087**, race-clustered bootstrap CI95 **[+0.0025, +0.0187]** (significant), Brier and ECE also better; LightGBM independent 1.91176 → 1.90439 (same direction, CI not significant on its 167 overlapping races); market-assisted arm **no gain** (-0.00018, CI straddles 0). **Retained as a candidate only — no arm beats the market (1.70403), NO-GO stands**; `horse_speed` untouched, no bundle changed. Full suite **2,441 passed**. Detail: stages/11.md, D43.

- Step 12 (**DONE**; engineering only, nothing promoted): protocol frozen in `reports/improvement/12/blend_manifest.json` BEFORE any fit, splitting step 10's development side into blend_dev (2026-02-01..06-12, 4,138 walk-forward OOS races — exponents only), calib (2026-06-13..08-07, 1,874 races — calibrators only, promotion rule committed before any eval scoring) and the reserved 618-race eval panel; final holdout never loaded. New additive module `models/blend.py` (normalized power blend, no price argument by construction) + 18 tests. **On 4,138 development races the optimal blend puts ZERO weight on the price-free model (alpha 0.0/beta 1.0 = the market alone)**; the best arm with any model weight (alpha 0.1/beta 1.0) gains +0.00148 log loss on eval (1.70255 vs market 1.70403) with race-clustered CI95 **[-0.00212, +0.00513]** — **no candidate demonstrably beats the market, NO-GO reconfirmed**. Calibration's real effect is on SELECTION: uncalibrated price-free flags 3,178/5,561 runners (57% of the field) at EV>=0.05 with A/E **0.540**; `OddsBandCalibrator` → 1,546 at 0.828; every market-combined line → ~353-369 runners at A/E 0.985-1.066. Executable-quote invariance PASSES on real data (max|delta| **0** for ability, reference prob and blend; reference-price control moves 0.572), as do the incomplete-book and non-runner probes. Step 10's baselines reproduce exactly, and **LightGBM's dev-OOS gap is closed** (re-scored from its saved booster over the full 5,561 rows, 1.91176). Full suite **2,459 passed**. Detail: stages/12.md, D44.

- Step 13 (**DONE**; engineering only, nothing promoted): added `models/train_xgboost.py` (mirrors `models/train.py`'s interface + `backtest/holdout.py`'s frozen load/score pair, CPU `tree_method=hist`) as a third boosted-tree candidate on step 10's frozen dev_core/dev_oos windows. Individual XGBoost scorecard (618-race eval panel): indep 1.97863 (gap -0.2745, the WORST of the four price-free candidates measured), market 1.71156 (gap -0.0075, between LightGBM's -0.0026 and CatBoost's -0.0152) — both NO-GO, matched 6-trial/5-fold tuning budget. Built a 6-fold whole-race walk-forward OOF panel over dev_core (fixed hyperparams, no per-fold re-tuning — 227,265/263,982 rows covered) to select simple ensemble-blend weights (grid, zero allowed) on step 12's own blend_dev window (4,138 races) and fit a calibrator on its calib window (1,874 races, genuinely out-of-fold) — **the log-loss-minimising search assigned XGBoost weight 0.0 in all 4 configurations tried** (indep/market x 2-model/3-model CatBoost+LightGBM), so the measured 3-model-minus-2-model log-loss delta on the reserved eval panel is exactly 0.0 (bootstrap CI95 [0,0]). Pairwise OOF diversity: indep branch correlations 0.72-0.80, market branch 0.85-0.95 (market price dominates all three, low added diversity). **XGBoost NOT retained; CatBoost+LightGBM (itself already NO-GO) remains the simplest matching candidate.** A mid-stage bug (bootstrap scoring raw, non-race-normalised probabilities) was found and fixed before evidence was finalized. Full suite **2,466 passed** (7 new tests). Detail: stages/13.md.

- Step 14 (**DONE**; extraction/archive infra only, nothing predictive wired in): repaired `llm/text_features.py`'s ground-excuse regex bug (ambiguous `(?:un)?suited by ... ground` misread a positive "well suited by the soft ground" as the excuse). Features are now tri-state (`FeatureValue(value: bool|None, evidence)`, unknown never coerced to False); Ollama claims need text-grounded evidence or are forced unknown; fixed a cache bug that mislabelled fallback-regex results as the configured (failed) backend. New append-only `text_archive` table (migration 6, same `data/races.db` as `odds_snapshots`) via `llm/text_archive.py` — `published_at` stays NULL forever when unknown, never backfilled. `scraper/spotlight.py::scrape()` archives every comment; `scripts/backfill_text_archive.py` imported the existing `data/spotlight.parquet` (**243 rows, one day 2026-06-17, 21 races — still a single-day sample, not a corpus**; idempotent, verified). 38 new tests. Full suite **2,493 passed**. `llm.enabled` stays false. Detail: stages/14.md, D46.

- Step 15 (**DONE**; regex+hosted measured, nothing wired in; local DEFERRED_DATA): resumed a prior session whose CLI process crashed mid-run (2 stuck ledger reservations, no stage note, no final scorecard) — verified and repaired rather than re-accepted blind. Fixed a real bug found live: DeepSeek V4.1 Flash (hosted, OpenRouter) is a reasoning model whose chain-of-thought counts against `max_tokens` — uncontrolled, it spent the WHOLE 800-token budget on reasoning and returned no answer on 176/243 real comments (`schema_valid_rate` 27.57%). Added OpenRouter's `reasoning:{"effort":"low"}` + raised `max_output_tokens` to 1600 → `schema_valid_rate` 86.01% AND cost/1000 DOWN $0.78→$0.37 (measured, both persisted as separate scorecards, D47). Also fixed: `reasoning_effort`/`max_output_tokens` missing from the hosted cache key (a rerun with a bigger budget was silently reading back a stale exhaustion failure); `extract()` now prefers OpenRouter's own reported `usage.cost` over a static price estimate (this model has weekday time-of-day price tiers a fixed price can misprice). Corpus unchanged from stage 14: 243 comments/1 date/1 source (DEFERRED_DATA vs the 500-1,000/multi-date/multi-source target); 60 reviewed labels are single-reviewer LLM-unverified, not human gold. Hosted finds real positives regex structurally cannot (0 regex recall on distance_excuse/trip_trouble/course_winner) at far higher cost/latency and 34/243 residual schema failures. Local Qwen3.5-9B/Hermes-4-14B **DEFERRED_DATA, Candidate enabled: no** — no Ollama runtime installed/reachable in this environment; installing one mid-session was judged disproportionate to the bounded stage budget. Identity-substitution probe unit-tested (mocked) but not run live: 0/243 real comments mention the horse's own name, so it would be a structural no-op. Programme-lifetime hosted ledger: $0.2836/$5.00, 498/1,200 requests, 0 stuck reservations. Full suite **2,512 passed** (19 new). Nothing promoted; `llm.enabled` stays false. Detail: stages/15.md, D47.

- Step 16 (**DEFERRED_DATA**, Candidate enabled: no): joined `llm.text_archive` against step 10's frozen matrix across its own frozen windows — **21 archived-text races in dev_core (all one day, 2026-06-17), 0 in dev_oos, 0 in final_holdout** (`reports/improvement/16/coverage_report.json`); the required chronological "later development windows" comparison cannot run on any genuine out-of-sample fold, so this is the prompt's own named data-deferred exception, not a rejected model finding. Built the guarded harness anyway: `features/text_features_v1.py` (versioned, optional join; tri-state/provenance preserved; point-in-time cutoff via `TextArchive.as_of`) — found and fixed a real bug live, the archive's `race_uid` casing (`norm_venue`-lowercased) doesn't byte-match the training matrix's casing, matching step 11's independently-discovered identical fix pattern (D48). `build_cache_only_hosted_backend()` reuses step 15's exact hosted config against a throwaway zero-budget ledger — a cache miss raises before any network attempt; live-verified 227/227 cache hits, **zero new hosted spend** (ledger unchanged $0.2836/$5.00, 498/1,200). `models.features.CANDIDATE_TEXT_FEATURE_COLS` is a new, deliberately isolated list (never merged into `CANDIDATE_INDEPENDENT_FEATURE_COLS`). Pre-registered `reports/improvement/16/frozen_protocol.json` for when real coverage exists. A smoke test (not the acceptance ablation) fit a price-free conditional logit on dev_core with the one archived day excluded, then scored that day: all 3 arms (no_text/regex_text/hosted_text) produced IDENTICAL log loss to ~1e-12 — the structurally-expected result of the text columns having zero training variance outside the single excluded day, itself evidence for why this is data-deferred. Full suite **2,524 passed** (12 new). `llm.enabled` stays false. Detail: stages/16.md, D48.
