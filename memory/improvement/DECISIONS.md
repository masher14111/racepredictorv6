# Durable decisions
Updated: 2026-09-19. Keep below 200 lines (target <=30); archive superseded rationale.
- D53 (step 20): The live-serving race identity fix is ADDITIVE, never a change to `execution.snapshots.race_uid`'s existing output — that function's exact value is the persisted, immutable content of every historical `odds_snapshots`/`paper_tickets` row (append-only, migration 4), so changing its format would silently reinterpret history. `execution.snapshots.race_key(venue, race_time)` is the new canonical identity (always venue-qualified, minute precision, matching `execution.race_facts.race_facts_key`'s scheme) and is used ONLY going forward, threaded through `execution.gates.race_key`, `Ticket.race_key`, and an optional narrowing filter on `SnapshotStore.quotes_as_of`/`latest_quote`/`best_quote`. `odds_snapshots` cannot be backfilled with the new column (migration 4's `RAISE(ABORT)` triggers forbid UPDATE) — its `race_key` is honestly NULL for every pre-migration row, a bounded one-time gap since new writes populate it going forward; `paper_tickets` (not append-only) IS backfilled, from its own already-stored `(venue, race_time)`. The off-time source-disagreement reconciliation (`features/_race_reconcile.py`) is wired ONLY into `features.builder.build_inference_matrix` (today's live rows), never `build_training_matrix` — it must never silently change any frozen dev/final-holdout matrix's race membership (D42). Tolerance is 180s, chosen well under the measured real floor (600s minimum gap between two distinct races at one venue/day, `data/unified_races.parquet`); a candidate merge additionally requires genuine runner-name overlap and is QUARANTINED (excluded, not guessed) when that overlap cannot be confirmed.
- D54 (step 20): The first version of migration 8 crashed applying it to the REAL `data/races.db` (4,806 existing `odds_snapshots` rows) with `IntegrityError: append-only: UPDATE forbidden` — a fresh empty test db never exercises this, because the backfill loop has zero rows to update against an empty table. Any future migration touching an append-only table (`odds_snapshots`/`text_archive`/`llm_shadow_extractions`, migrations 4/6/7) must be tested against a POPULATED copy of the real store, not just a fresh one, before being trusted. Every `ALTER TABLE ADD COLUMN` in `utils/storage/migrations.py` must additionally be idempotent against a partially-applied prior attempt (`PRAGMA table_info` existence check before each ALTER) — SQLite has no `ADD COLUMN IF NOT EXISTS`, and DDL auto-commits immediately outside the migration's own version-bump transaction, so a crash between two ALTERs is a real, not theoretical, restart state. Backup-before-migration on the live store, with a documented rollback (restore the file), is now the standing procedure for any future migration applied outside a fresh test db (`reports/improvement/20/MIGRATION.md`).
- D52 (step 19): Hosted shadow extraction is a distinct config switch (`llm.hosted_shadow.enabled`) from `llm.enabled`/`llm.backend` (Stage 14's numeric-feature gate) — flipping one must never flip the other; proven by test. A new append-only `llm_shadow_extractions` table (migration 7) separates `text_fetched_at`/`text_published_at` (copied from `text_archive`) from its own `extraction_time` (when the LLM call actually happened), and `ShadowExtractionStore.as_of()` filters ONLY on `extraction_time` — this is what stops a batch run well after a race went off from being read back as though the extracted FEATURE (not just the underlying text) existed pre-race. `scraper/spotlight.py::scrape()` had never been called from any daily entrypoint before this stage (grepped, confirmed) — the text archive was frozen at its single 2026-06-17 sample for that reason, not because nothing new existed; now wired into `scripts/refresh.py::_scrape()` alongside best-effort `_shadow_extract_text()`. Pricing is re-verified live on every run (`llm/shadow_extraction.py::verify_current_pricing`, cached-snapshot fallback on a transient fetch failure, fails closed with neither) rather than reused from Stage 15's frozen snapshot, since the shared ledger/cap persists across the whole run history. A cache-hit extraction replays its ORIGINAL call's `cost_usd` (informational, unchanged from `llm/hosted_adapter.py`'s existing behaviour) but must NOT be re-summed into a run's own spend total, or a 100%-cache-hit batch would misreport nonzero new spend — `run_shadow_extraction`'s `cost_usd_this_run` explicitly excludes cache hits. Live-verified today: pricing exactly at the $0.30/$1.20-per-million ceiling (unchanged since Stage 15); ledger $0.2835941872/498 -> $0.2837193322/499 requests (one genuine fresh call on an isolated store, plus a production batch of 3 that was 100% Stage-15 cache hits); remaining $4.7162806678/701. Nothing promoted; `llm.enabled` stays false.
- D01: Use 18 bounded prompts; one fresh chat can execute one step with shared filesystem memory.
- D51: User appended automatic stages19-24 on 2026-09-19 for capped DeepSeek shadow integration and reliable daily paper evaluation. Preserve original01-18 outcomes; use Sonnet5 High for19-23 and Opus5 xhigh for24. Existing hosted usage counts towards the SAME $5/1,200 cap; no numerical text-feature promotion or formal-window override. Scope: docs/improvement/DAILY_PAPER_FOLLOWUP.md.
- D02: AGENTS.md owns work rules; CLAUDE.md bridges Claude; DESIGN.md owns stable contracts.
- D03: STATE.md is progress/evidence; HANDOFF.md is immediate resume; dated stage files preserve history.
- D04: All active memory and stage notes stay below 200 physical lines; long evidence is linked.
- D05: Root historical HANDOFF/MEMORY remain preserved; older GO/model claims cannot override current evidence.
- D06: Sequential execution is the default. Parallel workers need isolated code/output ownership.
- D07: Keep CatBoost and grouped-softmax LightGBM; improve data/evaluation before optional model complexity.
- D08: Research completion can reject a candidate; unknown/missing data never becomes a fabricated pass.
- D09: Preserve existing champion artifacts; all new experiments use separate candidate run directories.
- D10: Freeze evaluation windows before modelling; July final-window results are already observed.
- D11: Preserve paper-only and current model/forward gates. Operational readiness != proven edge.
- D12: LLMs provide evidence-grounded text features only; benchmark extraction and downstream lift separately.
- D13: Benchmark repaired regex, local Qwen3.5-9B and hosted DeepSeek V4.1 Flash; Hermes remains optional, none a measured winner.
- D14: Model/effort assignments are practical recommendations, not comparative benchmark claims.
- D15: Model/effort must be chosen in the app; prompt text does not guarantee a selector change.
- D16: No scheduled automation or messages are created by writing this pack; step18 establishes bounded collection.
- D17: User requested XGBoost consideration; step13 explicitly tests its incremental ensemble gain, not forced inclusion.
- D18: User authorized a NASC-style sequential CLI runner; each fresh worker completes one stage, supervisor validates before advancing.
- D19: Hosted trial uses an existing local key, at most US$5 total / 1,200 calls with persistent accounting; missing access cannot become a pass.
- D20: Codex hit its usage limit before step01; explicitly route its assigned stages to Claude Sonnet 5 until the override is removed.
- D21: `race_uid` is the canonical race identity for all future steps; `race_id` is a dead, entirely-null column and must not be used for joins or split checks.
- D22: All chronological splits (train/test, calibration, early-stop, tuning CV) must cut on whole `race_uid` groups via `models/split_utils.py`, never on row count/index; hyperparameter tuning must run only on the post-calibration-carve core, never on rows that later become the calibration or outer-test slice.
- D23: `features/fuse.py` keys on `(race, horse_id, market_type)`, not just `(race, horse_id)` — a runner legitimately carries one row per market (WIN + PLACE), and any per-race grouping (`features/derive.py::_race_cols`) must also split by `market_type` or it mixes two different price books.
- D24: CatBoost (`models/train.py`) trains/tests all three targets (won/placed_2/showed) on `market_type == "WIN"` rows only, matching `models/train_lgbm.py`'s existing WIN-only population — live inference only ever serves WIN rows, so training any target on PLACE-priced rows would be a train/serve mismatch, not a place-market improvement.
- D25: Un-collapsing WIN/PLACE rows (D23) exposed that every `_trailing_fast`-based feature (trailing win/place rates, run counts, days-since-last-run, career runs, speed/form trends) assumes one row per runner per race and now double-counts. Verified real (not theoretical) on `days_since_last_run`/`horse_career_runs`. Deliberately NOT fixed in step 03 (out of "market fusing" scope) — assigned to step 04, which already requires steps 02+03 as prerequisites. `data/audit/03/training_market_fixed.parquet` must not be used for any retrain before step 04 lands.
- D26 (step 04): Every trailing/window aggregate (`features/_trailing_fast.py`'s `windowed_rates`/`windowed_mean`/`windowed_win_rate_days`, and `features/derive.py`'s `add_days_since_last_run`/`add_career_runs`/`add_class_change`) now collapses a runner's same-real-race market-duplicate rows (via `_race_event_key`/`_dedupe_group_ids`, keyed on `race_uid`) to ONE prior event before computing any window, then broadcasts the result back to every market row of that race. This both fixes D25's double-counting and closes a same-race self-leak (a row could see its own WIN/PLACE sibling of the race being predicted as a spurious "prior" result). `features/_trainer_form.py`/`_jockey_form.py` were refactored onto the same shared primitive (`windowed_win_rate_days`) instead of keeping their own independent, unfixed day-window loops.
- D27 (step 04): `race_complexity` (legacy) mixed a market-entropy component into a feature `models/features.py` lists as price-free/independent, and standardized every component against the mean/std of whatever frame happened to be passed to `add_race_complexity` (recomputed live, not a frozen constant) — an admissible historical row's value could shift when later rows were appended to the same build call, and train vs. a pruned inference call saw different scales. Left byte-identical (every existing model bundle's `meta["feature_cols"]` is bound to its original meaning — D9). `race_complexity_v2` (independent: field size + class/form/rating spread only, standardized against a FROZEN train-fitted scale persisted at `data/feature_artifacts/race_complexity_v2_scale.json` via `features/_feature_scale.py`) and `race_market_entropy` (explicit market-assisted companion) are additive replacements, produced by `features/builder.py` but not yet in `FEATURE_COLS`/`PRICE_FREE_FEATURE_COLS` — adoption is step 10's corrected-baseline retrain, not this step.
- D28: Keep numbered coding stages sequential in the shared checkout. Use a two-worker CPU budget for verified-safe independent targeted tests/checks; keep the full suite serial because its process, port and shared-file tests are not xdist-safe. Six workers caused multi-gigabyte duplication and was slower; two workers improved the measured feature/tools group from 21.86s to 14.50s.
- D29 (step 05): `scraper/timeform_historical.fetch()` (an existing, already-priority-3-in-fuse racecard provider) is the source of TRUTH for today's declared jockey/trainer/draw/weight/OR/equipment/runner_status — it is now called from `scripts/refresh.py`'s daily `_scrape()` (best-effort, like the 3 odds scrapers). `features/builder.py::_fill_last_known_connections` tags every live connection with a `{role}_connections_source` in {"declared","historical_fallback","missing"}; only a value ALREADY present before the fallback fill (i.e. reported by today's own fused row) may be "declared" — a value invented from the horse's own past runs is always "historical_fallback", never presented as equivalent. Historical/labelled rows default this flag to "declared" (they are real recorded results), preserving train/serve column parity.
- D30 (step 05): Timeform's native per-event/runner/jockey/trainer ids live under `src_race_id`/`src_horse_id`/`src_jockey_id`/`src_trainer_id` (provenance only); the canonical `horse_id`/`jockey_id`/`trainer_id` used for cross-source joins stay name-derived (`utils.normalizer._canonical_id`) so a Timeform row and an odds-source row for the same runner reconcile to the same canonical id despite different native id schemes. `race_id` remains the dead column per D21 — do not repurpose it for Timeform's native id.
- D31 (step 06): Betfair's daily SP price file is published one calendar day AFTER the local race date it actually contains (`dwbfpricesukwin18092026.csv`'s own `event_dt` rows are all `17-09-2026`; reproduced on 4 consecutive live days). `scraper/betsp_historical.py::_fetch_one` now adds `_BACKBONE_FILE_DAY_OFFSET` (1 day) when building the request URL, so `_fetch_backbone(cfg, days)` returns rows whose ACTUAL race_date matches `days`, aligning with `_fetch_enrichment`'s results-site rows (fetched for the literal `days`, no offset). Uncorrected, a short/bounded window (`scripts/fetch_results_window.py`) requested backbone and enrichment for the same nominal day but got disjoint actual dates, joining 0% of positions — reproduced live this stage, see stages/06.md.
- D32 (step 06): `scraper/betsp/writer.py::write` no longer does plain "last write wins" dedupe on `(race_date, venue, horse_id, market_type)` — it sorts existing+incoming rows by `position.notna()` first, `fetched_at` second, before `drop_duplicates(keep="last")`, so a resumable re-fetch that returns an incomplete/failed enrichment for an already-resolved day can never regress a known finishing position back to null. Found by reproducing D31 live: an uncorrected 1-day backfill call actually zeroed out `data/historical/betsp.parquet`'s 2026-09-17 position fill (640/836 -> 0/836) before both fixes landed; restored and reverified equal (640/836) after the fix. Any other `parquet_store.append_parquet` caller (e.g. `scraper/timeform/writer.py`) is untouched — "freshest declaration wins" is correct there, only the results store needs completeness-aware merge.
- D33 (step 06): `going_speed` (in `FEATURE_COLS`, frozen for existing bundles) only ever matched a going string that was byte-identical to one of `going_speed_map`'s hyphenated keys, missing every real "X to Y"-spaced/parenthetical/Irish-terminology going string (measured 20.92% fill vs going_band's ~100%, using a *different*, already-correct coarse-band derivation). `features/derive.py::add_going_speed_v2` is an additive companion (same D26/D27 pattern): same frozen `going_speed_map` numbers, corrected string matching (strips parenthetical/slash qualifiers, recognises Irish "yielding"/"good-to-yielding" as the soft/good-to-soft equivalents, takes the headline band of a two-band transition string). All-weather going terms (standard/slow/fast) are never coerced onto the turf scale — flagged via a separate `going_is_all_weather` indicator instead. Both are additive in `CANDIDATE_INDEPENDENT_FEATURE_COLS`, not yet adopted into any trained bundle's `FEATURE_COLS` — adoption is step 10's job.
- D34 (step 06): `tools/coverage_report.py` is the reusable coverage/freshness report (row/column counts, date range + staleness, all-null columns, watched-column fill, per-source/region breakdown, missing-calendar-day detection, sudden provider-share-shift alerts) for any of the app's dated parquet stores; JSON output is meant to be diffed before/after a pipeline change rather than re-derived ad hoc each stage. `data/audit/06/training_refreshed.parquet` is a candidate (not the frozen baseline) rebuilding the labelled matrix over the full current `data/unified_races.parquet` through the current pipeline — extends measurable coverage past the frozen baseline's 2026-07-25 cutoff; adoption into training is step 10's job, same as D26/D27/D33.
- D35 (step 06, second attempt): Validating the D34 candidate against the frozen baseline found `data/historical/betsp.parquet` had lost all 1,010 positions for 2026-07-25 (single `fetched_at=2026-09-18T22:29:20Z`, predating this session — an uncorrected `scripts.fetch_results_window` re-run hitting the same D31/D32 failure mode for a day nobody re-checked). Restored the 501 WIN-market positions from the frozen baseline's own ground truth, matched market-type-EXACT on `(venue, race_time-minute, src_horse_id, market_type)` — NOT cross-market, even though raw WIN/PLACE rows do carry an identical position when both are independently resolved (verified on 2026-07-20: 195/245 pairs, 100% equal): the frozen baseline never has both markets resolved for the same race+horse simultaneously, so that equality isn't the ground truth actually being restored from. `features/fuse.py`'s existing `_MARKET_SPECIFIC_COLS` cross-fill (position deliberately excluded — DESIGN.md) then correctly broadcast the fix onto the sibling PLACE rows on its own. Re-ran `utils.normalizer.normalize(write=True)` afterward so `data/unified_races.parquet` picked up the fix (it's a derived cache, not live). Backups of both pre-repair stores at `data/audit/06/backups/*.pre_20260725_repair`. 13 other pre-existing low-fill days (fetched_at 2026-06-14/2026-07-27, both before step01) have no frozen-baseline ground truth and were left unrepaired — documented, not fabricated. Detail: stages/06.md.
- D36 (step 07): `scripts/daily_paper_loop.py::_today_tickets` must never reuse `_day_start`'s day-truncated `now` (needed only so `build_ticket`'s idempotent, issued-at-hashed ticket_id is stable across same-day re-runs) as the gate's decision instant — a runner's own cached price is fetched intraday, so evaluating its age against midnight makes it negative and trips `execution/gates.py`'s `executable_price:timestamp_in_future` guard on every candidate, unconditionally. `_today_tickets` now takes `now` (issuance) and a separate `evaluated_at` (the real wall-clock instant, used for `evaluate_race`'s freshness arithmetic and for the point-in-time `SnapshotStore.latest_quote(..., as_of=evaluated_at, market_type="WIN")` lookup that is now threaded into `evaluate_race`'s `quotes=` — previously unwired, so live pricing fell back to an untracked `predictions.json` cache instead of the tested, staleness-checked store). Masked in production only because the model verdict is NO-GO (every candidate is forced PASS regardless of these reasons); would have silently vetoed every live bet the moment the model ever reaches GO. Detail: stages/07.md.
- D38 (step 09, independent audit): `features/_trailing_fast.py::_dedupe_group_ids` must key the market-duplicate collapse on `(entity keys..., RUNNER, race event)`, never `(entity keys..., race event)` alone. The thing being collapsed is "one runner, one real race, two market rows" — that is only what `(entity, race)` means when the entity IS the runner. For a trainer- or jockey-keyed window it silently deleted every runner but one from a yard's multi-runner race, keeping whichever row happened to come first in the frame: measured on `data/audit/06/training_refreshed.parquet`, 5.94% of `(trainer_id, race_uid)` groups hold more than one distinct horse, **17,962 real trainer runner-events (6.50%) were discarded**, `trainer_win_rate` differed on 20.65% of rows from a runner-aware collapse (max |delta| 0.75) and moved on 25.88% of rows under a pure row shuffle. New `_runner_key()` supplies the runner component (`horse_id`, falling back to `horse_name`; a null id gets a row-unique token so two unknown runners never merge) only when the runner is not already an entity key, so horse-keyed windows are unchanged by construction. After the fix: 0 runner-events lost, 0 rows differing. `data/audit/04/training_pit_fixed.parquet` and `data/audit/06/training_refreshed.parquet` predate this and must be REBUILT by step 10, not reused. Detail: stages/09.md, reports/improvement/09/findings.md F2.
- D39 (step 09, attempt 2): every trailing/window prior cut in `features/_trailing_fast.py` runs on the real OFF-TIME, never on a row position or on the date-only `race_date`. `_order_instants` recovers the off-time from the race-event key (`race_uid` = venue + minute-resolution off-time, parses on 100% of both matrices' rows) and returns a `self` instant (when this row runs) and a `prior` instant (when this row becomes an observable result); an event key WITHOUT an off-time gets start-of-day for `self` and end-of-day for `prior`, so an unknown slot is treated as unavailable information in both directions instead of being guessed as midnight. `_chrono_rank` orders on (prior instant, event, runner) with content-derived `factorize(sort=True)` codes, so the `lookback_runs` tail cap can no longer cut arbitrarily into a block of same-instant rows. Measured before: `trainer_win_rate` counted 126,408 same-day priors that ran LATER (46.48% of its same-day priors, 25.36% of runner-events), `jockey_win_rate` 132,594 (49.95%, 29.98%), and 21.94% of values moved under a pure row shuffle. After: **0** later-running priors, **0** row-order sensitivity on all 10 measured window paths, while 251,361 trainer / 265,323 jockey genuinely-earlier same-day priors still count. `features/builder.py::build_inference_matrix` correspondingly admits today's ALREADY-RUN races (`position.notna()`, previously `race_date < today`), because training counts an earlier-that-day result and the serving path must see the same history; a race that has not run still carries no position and cannot enter. Feature impact step 10 inherits: `trainer_win_rate` moved on 20.59% of rows, `jockey_win_rate` 20.91%, `historical_win_rate` 0.00% (a horse runs once a day). `strict=True` (the engine stages) keeps its date-level cut and is unchanged in meaning. Detail: stages/09.md, reports/improvement/09/findings.md F1.
- D40 (step 09, attempt 2): a feature computed FROM the price book is not price-free merely because it is not itself a price. `models/features.MARKET_DERIVED_FEATURE_COLS` (currently `["race_complexity"]`) is stripped from `PRICE_FREE_FEATURE_COLS` alongside `PRICE_FEATURE_COLS`, and `audit/leakage.py::check_provenance` fails if any member leaks back in. `race_complexity` stays in `FEATURE_COLS` and every trained bundle keeps its own `meta["feature_cols"]` (verified: `catboost_v3nf_meta.json` carries its own 40-column list), so no artefact or serving path changes — but it also means every "independent" line already saved in `reports/forward_validation_20260918.md` was fitted with `race_complexity` in scope and must NOT be cited as a price-free baseline. Adopting `race_complexity_v2` (D27) remains step 10's option, not a requirement of this strip. Verified by re-running the price-perturbation probe: no price-free or candidate-independent feature reacts.
- D41 (step 09, attempt 2): any integer duration compared against `DatetimeIndex.asi8` must be built from that index's OWN resolution — `features/_trailing_fast._ticks_per_day` — never a hard-coded nanoseconds constant. pandas 3 parses this project's `race_date` at microsecond resolution, so the previous `window_days * 86_400_000_000_000` made the trainer/jockey day windows 1,000x too long: the nominal 14-day "hot" window averaged 108.03 priors and was identical to its own 90-day baseline on 100.00% of comparable rows, so `trainer_hot_strike_rate`/`jockey_hot_strike_rate` were all-history rates and `{trainer,jockey}_form_zscore` was structurally ~0 — four `FEATURE_COLS` members with no signal. After the repair the 14-day window averages 18.11 priors vs 79.80 for 90 days and `trainer_form_zscore` has std 0.91. Regression test is parametrised over `s`/`ms`/`us`/`ns` frames so a future pandas resolution change cannot silently reintroduce it.
- D42 (step 10): the experiment protocol is frozen in `reports/improvement/10/run_manifest.json` BEFORE any candidate is trained — data/code hashes, eligible population (`market_type == "WIN"`, `race_uid` grouping), dev_core (2024-01-01..2026-08-07), dev_oos (2026-08-08..2026-08-27, the development scorecard slice ALL candidates share), and `final_holdout_RESERVED_UNTOUCHED` (2026-08-28..2026-09-17, step17's single frozen-selection evaluation only). `models/conditional_logit.py` (race conditional-logit) and `models/split_utils.py` (whole-race-group chronological/walk-forward splitters, D22's enforcement point) are new production modules this stage introduced. The first genuinely price-free scorecard (market-only, conditional-logit, CatBoost, grouped-softmax LightGBM, each independent + market-assisted) scored all 6 candidates on the identical dev-OOS panel: every candidate remains NO-GO (market log loss 1.70403; independent branches -0.21..-0.23 gap, market-assisted -0.003..-0.015 gap) — the historical NO-GO verdict is reconfirmed, not overturned, on repaired data. A development-fold-only weighting ablation found unweighted/race-weighted sample weight beats the existing odds-inverse scheme by ~0.14 log loss on the conditional logit — disclosed, not adopted (no production weighting changed this stage). Detail: stages/10.md.
- D43 (step 11): measured performance is sourced from the raw Sporting Life archive (`data/historical/raw/sporting_life`, already trusted for settlement by `execution/race_facts.py`), never from `horse_speed` — which stays a finishing-position percentile proxy, byte-identical, because every trained bundle's `meta["feature_cols"]` is bound to its current meaning (D9). `features/measured_timing.py` owns the ingestion contract: units are yards/seconds/lengths/yards-per-second and carried in each column name; a non-runner is excluded (no performance exists), a runner that did not complete gets a **null** time and is never imputed nor treated as a slow finisher; a race missing a parseable winning time or distance emits nothing; `available_from` is the race's own local off time, so a measured result is observable only after its race has run (the same rule D39 applies to `position`), and the join key reuses `race_facts`'s normalised venue + minute-resolution local off time so both views of a race reconcile. `finish_distance` is the margin to the horse IN FRONT (incremental) — established by measurement, not convention: 373 sampled races contain a decrease down the finishing order, impossible under a cumulative reading, vs 34 merely compatible — so beaten-lengths-behind-winner is a running sum, and one unreadable gap nulls every later runner rather than under-reporting the deficit. **Only the winner's time is published**: a beaten runner's time is reconstructed through a DECLARED `lengths_per_second` (not fitted — there is no within-race time spread to fit against), so every column derived through it is named `est_*`. `features/measured_speed.py` fits pars as strictly-prior mean/std of `log(winner speed)` over whole same-instant blocks with an 8-level course/surface/race-type/distance/going backoff (≥15 prior races, `par_level` recorded), which is what makes the family both append-invariant and row-order-invariant — verified on real data, 527,220 and 551,815 rows, 0 differences. The 7 `msf_*` pre-race features are strictly-prior horse aggregates via `_trailing_fast` (inheriting D26's market-duplicate collapse and D39's off-time ordering) and live in `MEASURED_SPEED_FEATURE_COLS` → `CANDIDATE_INDEPENDENT_FEATURE_COLS` only, the same additive switch as D27/D33 — no `FEATURE_COLS` membership, no bundle change, no promotion. **Sectionals are DEFERRED_DATA (Candidate enabled: no)**: the archive contains no sectional/split/furlong-time field at all; they must come from a licensed feed, never from synthesis. Evidence for retaining the family: on step 10's frozen windows and identical 618-race dev-OOS panel (all three baselines reproducing step 10 exactly), the price-free conditional logit improves 1.92141 → 1.91087 with a race-clustered bootstrap CI95 of [+0.0025, +0.0187], better Brier and ECE, and LightGBM moves the same way; the market-assisted arm gains nothing (-0.00018, CI straddles zero), i.e. the market already prices this. It closes ~0.010 of a ~0.217 price-free gap and **no arm beats the market — NO-GO is reconfirmed, not overturned**. Detail: stages/11.md.
- D44 (step 12): probability combination is a function of exactly TWO probability vectors — the independent (price-free) estimate and the de-vigged REFERENCE market line — and of nothing else. `models/blend.py::power_blend(p_model, p_reference, race_ids, alpha, beta)` has no price argument at all, so "an executable bookmaker quote cannot move estimated ability or the reference probability" is structural, not a test outcome (verified anyway on real data: perturbing only `odds_decimal`/`sp` over 710 rebuilt races moves the price-free prob, the reference prob and the blend by max|delta| **0**, while the reference-price control moves the reference line by 0.572 — 13 market-assisted features do react, by design). A race with ANY missing/non-finite reference or model probability is emitted as NaN for EVERY runner (mirrors `models.devig.devig`) — a blend over an incomplete field is not a probability vector. Selected on step 10's frozen development side, split into blend_dev (2026-02-01..06-12, 4,138 walk-forward OOS races, exponents only), calib (2026-06-13..08-07, 1,874 races, calibrators only, promotion rule committed before any eval scoring) and the reserved eval panel (step 10's 618-race dev-OOS); the final holdout was never loaded. **Outcome: the unconstrained grid puts ZERO weight on the price-free model (alpha 0.0/beta 1.0 = the market alone); the best arm with any model weight gains +0.00148 log loss on eval with a race-clustered CI95 of [-0.00212,+0.00513] — no candidate demonstrably beats the market, NO-GO reconfirmed.** Calibration's real effect is on selection, not on log loss: the uncalibrated price-free line flags 3,178/5,561 runners (57% of the field) as EV>=0.05 bets at A/E 0.540, `OddsBandCalibrator` cuts that to 1,546 at 0.828, and every market-combined line selects ~6% of the field at A/E ~1.0. Separately AUDITED and deliberately NOT fixed: `models.predictor._score_value` feeds `_apply_fl_recalibration` the EXECUTABLE best-of-N board price (`_effective_decimal`) while `models/fl_oddsband_v3nf_calib.pkl` was fitted on the REFERENCE price (ppwap) — measured on 308 real live runners, a +10% executable-quote move alone shifts the served `value_win_prob` by mean 0.00793/max 0.04319; `value_win_prob_independent` (what `execution.gates` prefers) is unaffected, so it is a fit/serve price mismatch, and repairing it is a live-serving change of the class step 08 deferred. Nothing promoted; `models/blend.py` is additive and unwired. Detail: stages/12.md.
- D45 (step 13): XGBoost was tested as a genuine third boosted-tree candidate (`models/train_xgboost.py`, own Optuna-tuned hyperparameters via the same whole-race chronological CV shape as `models/tuner.py`'s CatBoost study, own calibrator) and **rejected as a blend member, not forced in and not forced out** — a log-loss-minimising grid search over blend weights (0 allowed, matching D17's "test the gain, don't force inclusion" instruction) on step 12's own blend_dev window (4,138 out-of-fold development races) assigned XGBoost weight 0.0 in all four configurations tried (indep/market x 2-model CatBoost+LightGBM/3-model +XGBoost), so the measured 3-model-vs-2-model log-loss delta on the reserved 618-race eval panel is exactly 0.0 (race-clustered bootstrap CI95 [0,0]) — not a near-zero noisy result, a structural consequence of the selected weight. XGBoost's own individual branches are also NO-GO on the same scorecard as the other three price-free/market-assisted families (indep 1.97863, the worst of four measured; market 1.71156, between LightGBM's 1.70664 and CatBoost's 1.71924) — reused step 10's frozen dev_core/dev_oos windows and step 12's blend_dev/calib sub-windows verbatim, no new boundary chosen. Model NOT retained; `models/train_xgboost.py` stays additive/candidate-only like `models/blend.py` (D44) and `models/conditional_logit.py` (D42) — no `FEATURE_COLS`/bundle/serving-path change. Detail: stages/13.md.
- D46 (step 14): a phrase extractor cannot use an optional prefix (`(?:un)?X`) for negation — `ground_excuse`'s `(?:un)?suited by the ... ground` matched the substring "suited by ... ground" inside BOTH "well suited by the soft ground" (no excuse) and "unsuited by..." (excuse). Fixed with paired `patterns`/`negative_patterns` per `TextFeature` (negative checked first, fixed-width lookbehind `(?<!not )(?<!never )` so "not suited by..." can't match the cancel pattern). Every feature is now tri-state (`FeatureValue(value: bool|None, evidence)`) — unmentioned stays unknown, never coerced False; only the LLM backend emits unknown (regex's "no match" honestly means False-no-evidence). `OllamaBackend` forces any `true` claim without a text-grounded `evidence` substring to unknown. Separately fixed: the extraction cache was keyed on the CONFIGURED backend even on failure, so a regex-fallback result could be read back mislabelled as a real Ollama result — `TextFeatureExtractor` now uses two distinct cache slots (`{backend}` vs `{backend}_fallback_regex}`). New append-only `text_archive` table (migration 6, same `data/races.db` as `odds_snapshots`) via `llm/text_archive.py`: `published_at` is NEVER backfilled from `fetched_at`/`archived_at`; `as_of()` filters on `fetched_at` (real scrape time), not write-time. `scraper/spotlight.py` archives via `execution.race_facts.race_facts_key(venue, race_date)` (not `execution.snapshots.race_uid()`, which would double-convert spotlight's already-local-minute timestamp). `scripts/backfill_text_archive.py` imported the existing 243-row/one-day/21-race `data/spotlight.parquet` (idempotent) — still a single-day sample; `llm.enabled` stays false.
- D47 (step 15): DeepSeek V4.1 Flash (hosted, via OpenRouter) is a reasoning model whose chain-of-thought is billed as `completion_tokens` against the SAME `max_tokens` budget as the JSON answer — with reasoning left uncontrolled, it spent the entire 800-token budget on reasoning and returned no answer content on 176/243 real archive comments (`schema_valid_rate` 27.57%, reconstructed post hoc from the real cached responses in `reports/improvement/15/scorecard_hosted_reasoning_unset_baseline.json`, no new spend). `llm/hosted_adapter.py::OpenRouterBackend` now sends OpenRouter's unified `reasoning: {"effort": ...}` field (default `"low"`, the model's lowest supported effort per its live `/models` `supported_parameters`) and `max_output_tokens` was raised 800→1600; measured on the full 243-comment corpus this cuts `schema_valid_rate` to 86.01% AND cost per 1,000 comments from $0.78 to $0.37 (lower reasoning is cheaper, not a quality/cost tradeoff here) — the residual 34/243 failures are the same exhaustion mode on unusually long comments, a genuine model characteristic, not a harness bug. Both `reasoning_effort` and `max_output_tokens` are now part of the hosted extraction cache key (`_cache_key`) — they were not before, so a rerun with a larger budget was silently reading back a stale exhaustion failure cached under the smaller one (reproduced once, then fixed). Separately, `extract()` now prefers OpenRouter's own reported `usage.cost` (requested via `"usage": {"include": true}`) over a static price×tokens estimate for the COMMITTED ledger amount, because this model's real price has weekday time-of-day tiers (2x peak vs off-peak, verified in `reports/improvement/15/openrouter_models_raw.json`) that a fixed caller-supplied price can only be accurate for at the instant it was measured; the reservation still uses the caller-supplied worst-case ceiling price (fail-closed, unchanged). Detail: stages/15.md.
- D48 (step 16): joining `llm.text_archive` (keyed via `execution.race_facts.race_facts_key`, which lower-cases the venue through `norm_venue`) against a training matrix's `race_uid` (original source casing, e.g. `"Ripon|..."` vs `"ripon|..."`) by byte equality silently matches ZERO rows even for a genuinely shared race — reproduced live (0/21 archived races matched before the fix). `features/text_features_v1.py::_archive_key()` re-normalises the matrix side (`norm_venue` + `minute_key`) before every archive lookup; this is the SAME mismatch step 11 independently hit and fixed joining measured-timing keys (`reports/improvement/11/02_build_experiment_matrix.py::race_key_of`) — any future stage joining a training-matrix `race_uid` against a `race_facts_key`-derived store must apply the same normalisation, not assume byte equality. Separately, `build_cache_only_hosted_backend()` proves a hosted-LLM cache can be reused with a hard, structural (not merely policy) zero-spend guarantee: constructing `OpenRouterBackend` with a throwaway, non-persisted `HostedBudgetLedger(max_cost_usd=0.0, max_requests=0)` makes a cache miss raise `BudgetExceeded` inside `ledger.reserve` BEFORE any network attempt is possible, so a caller can safely reuse frozen extractions from a prior stage's paid run (step 15's, verified 227/227 cache hits, $0 new spend) without any risk of accidental new billing. Step 16 itself is DEFERRED_DATA: joining the archive against step 10's frozen dev_core/dev_oos/final_holdout windows found 21 archived-text races in dev_core (all one day) and ZERO in dev_oos or final_holdout, so the required chronological "does text improve FUTURE forecasts" comparison cannot run on any genuine out-of-sample fold — confirmed structurally (not just by absence) by a smoke fit: excluding that one day from training leaves every text column 100% missing across all 263,755 training rows, so `ConditionalLogitModel`'s nanmean/nanstd standardisation collapses it to a constant zero-contribution feature and the fitted weight cannot be anything but ~0. `models.features.CANDIDATE_TEXT_FEATURE_COLS` is a new, deliberately isolated list — never merged into `CANDIDATE_INDEPENDENT_FEATURE_COLS` (unlike step 11's measured-speed family, which had positive ablation evidence) — until a stage with real multi-day/multi-source coverage re-runs `reports/improvement/16/frozen_protocol.json` for real. `llm.enabled` stays false.
- D49 (step 17, independent readiness review): (a) "artifact untouched" must be proven by a CONTENT HASH, never by `git status` — `models/*.bin|*.pkl|*.json|*.txt` are gitignored, so steps 10-13's "git status shows champions unmodified" was a vacuous check (the claim itself held by mtime; sha256s now live in `reports/improvement/17/candidate_manifest.json`). (b) Repairing a feature UNDER a frozen served bundle creates train/serve skew, so every such repair obliges a re-score of the served bundle through the repaired pipeline: D41 turned four features from structurally-dead (std 0.000 in the champion's own `models/reference_features.parquet`) into live signals (std 0.91-0.94) and shifted `trainer/jockey_hot_strike_rate` by PSI 0.43-0.44 (the model gate's own retrain threshold is 0.2), and the served champion's price-free line scores 2.10003 vs 1.93620 for step 10's same-family rebuild on the identical 618 races (paired race-clustered CI95 [-0.222,-0.107]), replicated on untouched data (-0.177, CI [-0.229,-0.125]). (c) The selection rule was frozen in `02_selection_protocol.json` BEFORE any champion score existed (default = keep the champion; switch only on a CI excluding zero), which is what makes the switch to the step-10 CatBoost rebuild (`s10idp` price-free primary, `s10mkt` disclosure) a finding rather than a preference. It is a RECOMMENDATION: nothing was promoted, the served bundle is the recorded rollback target, and the rebuild carries only a `won` target, so swapping the headline/place bundle needs a retrain work order, not a step-18 integration fix. (d) Step 10's reserved final holdout was opened exactly once, after the manifest freeze, by a script that first reproduced the development panel to max|delta| 0 through the identical code path and that refuses a second run: the frozen candidate FAILS against the market (price-free 2.01029 vs 1.76165, deficit CI95 [+0.204,+0.291]; market-assisted 1.76795, CI straddles 0). NO-GO is confirmed on untouched data, **no untouched holdout remains, and all further evidence must be prospective** — the July 2026 window stays already-observed. (e) Two fail-OPEN paths were closed, both strictly stricter: `ExecutionConfig._apply_provisional_limits` now clamps `gates.require_model_validation` on at load exactly like `forward_gate.require_model_go` (one YAML line had issued a CANDIDATE under the real NO-GO verdict, against `execution/gates.py`'s own "deliberately no override"; an in-memory `dataclasses.replace` stays available to tests/analysis), and any timestamp parse feeding a freshness gate must pass `format="mixed"` and treat a present-but-unreadable value as a fail reason — under pandas 3 `models.predictor._race_ev_gate` read a 2-hour-stale sub-second row as NaT, `ages.max()` skipped it, and the race stayed EV-eligible. (f) Technical readiness to COLLECT, readiness to run a FORMAL window, and evidence of EDGE are three separate verdicts and are recorded separately: capture-only shadow collection is READY; a formal window / GO-mode is NOT (live loop sizes no stake so a winner settles profit 0.0, `settle_ticket` is never called live, PASS rows are never settled so the ledger accrues no CLV); edge is undemonstrated. A reviewer's checks never open a real store read-write (`mode=ro`) and replays run on isolated copies with the real stores hashed before and after. Detail: stages/17.md.
- D50 (step 18): a live-scraped column that is null on every row (e.g. `runner_status` while no declared card has arrived, B8) converts through pandas' pyarrow dtype backend to `null[pyarrow]`, not `object`; `.fillna("")` on that dtype raises `pyarrow.lib.ArrowInvalid: Invalid null value` instead of substituting the fill value, because boxing `""` into a null-typed array is itself invalid. This crashed `scripts.refresh`'s live `[4/5] predict` step in `models/predictor.py::_is_non_runner` on this session's first real capture attempt — step 17's replay never hit it because replay used static parquet fixtures, not a freshly-scraped live frame. Fixed by inserting `.astype(object)` before `.fillna("")` on both the `runner_status` and `jockey_name` reads; this only changes null-to-string normalization ahead of a string comparison, not any feature, model, or served bundle. The existing `test_null_runner_status_keeps_the_whole_field` set the column via a Python `None` scalar (plain `object` dtype) and did not catch this; a new regression test constructs the real `null[pyarrow]` dtype via `convert_dtypes(dtype_backend="pyarrow")`. Detail: stages/18.md.
- D37 (step 08): `scripts/daily_paper_loop.py::_index_results`/`_settle_open_tickets` joined an open ticket to its historical result on `horse_id` **alone**, globally, no venue/date restriction — `by_id[str(horse_id)] = row`, last write wins across the whole results list. Verified 88% of `horse_id` values in the real store recur across more than one `race_date`, so this was the common case, not an edge case; reproduced settling a losing ticket "won" from an unrelated race's result. Fixed with a canonical `(venue, off-time-to-the-minute, horse)` key built independently of `execution.snapshots.race_uid` (see below) — day/venue-scoped fallbacks only when a result carries no time-of-day; a key with two disagreeing results is dropped from its index and the ticket is left open rather than guessed. Also fixed `_paper_summary` (both `daily_paper_loop.py` and `paper_betting.py`) computing `n_open`/`n_settled` over every ticket row regardless of `decision` — on a NO-GO day every decision is PASS (no stake), so the previously-reported "988 open tickets" was always 988 disclosure records, 0 real candidates; now `n_pass`/`n_candidates` are reported separately and `n_open`/`n_settled` are scoped to CANDIDATE only. Two further real defects were found and **deliberately deferred**, not fixed: (1) `execution.snapshots.race_uid()` drops venue once an off-time parses, confirmed 4/35,107 real cross-venue collisions (~0.011%) on the store — a fix needs an on-disk schema/data migration across `odds_snapshots`/`paper_tickets`, out of proportion to a CLV-only exposure; (2) the live loop's settlement never calls `execution.settlement.settle_ticket` (Rule4/dead-heat/each-way/non-runner) — only `execution/simulator.py`'s offline backtest does — even though `execution.race_facts`'s Sporting Life archive has real, current coverage (verified 2 days before this session's "today"); wiring it in is a live-settlement-path architecture change beyond this stage's identity/reporting acceptance criteria. Detail: stages/08.md.
