# 17 — Review the integrated candidate for paper operation

2026-09-19 (Claude Fable 5.1, xhigh; automatic supervisor stage 17, attempt 1). **DONE** (review complete).
Model verdict: **NO-GO / PAPER-ONLY**, reconfirmed on untouched data. Forward validation: not started.
Nothing promoted. Reviewer role: independent — every figure below was re-derived in this session from
the artifact itself; no stage note was accepted as evidence. No prior stage-17 artifact existed.

## Readiness decision (full text: reports/improvement/17/READINESS.md)

| Question | Answer |
|---|---|
| A. Technically ready to COLLECT paper data (capture-only/shadow, NO-GO)? | **READY**, conditions C1-C3 |
| B. Ready for a FORMAL evidence window, or to operate under a future GO? | **NOT READY** — blockers B1-B3 (B4-B8 before evidence counts) |
| C. Evidence of predictive improvement or profitability? | **NONE** — frozen candidate fails vs market on the holdout |

Real-money execution disabled; no bookmaker order-placement code exists in the repo (traced + grep).

## Scope 1 — optional dispositions 11..16 (`01_verify_dispositions.py`, 27/27)

All six have a ledger disposition and a stage note; 16 (DEFERRED_DATA) carries `Candidate enabled: no` +
`Resume condition:`. Independently confirmed: step 10's matrix sha256 == its frozen manifest; the
partitioned `unified_races` hash is unchanged; the only manifest code file that drifted is `models/features.py`
(additive, steps 11/16); the 618-race / 5,561-row panel lies wholly inside dev_oos; **no stage artifact holds a
model score on a holdout row**; `MEASURED_SPEED_FEATURE_COLS` / `CANDIDATE_TEXT_FEATURE_COLS` are absent from
the served lists; `llm.enabled` false; no candidate module is imported at top level by serving except
`features.text_features_v1` (a constant list, via `models/features.py`; its hosted-LLM imports are lazy);
hosted ledger $0.2836/$5.00, 498/1,200, 0 outstanding. No disposition was invented or rewritten.

**Evidence-method flaw found:** `models/*.bin|*.pkl|*.json|*.txt` are **gitignored**, so steps 10-13's
"`git status` shows champions unmodified" was a vacuous check. The claim itself holds (every mtime predates
2026-09-18; the F-L calibrator's sha256 matches its own meta record). Real sha256s are now in the manifest.

## Scope 2 — candidate selection; the route traced

Route read end to end (two line-referenced traces, key claims then verified by me — two agent numbers were
WRONG and are corrected below): `scripts/refresh.py` -> `utils.normalizer` -> `features.builder.build_inference_matrix`
-> `models.predictor` (CatBoost v3 x3 targets, price-free v3nf + F-L remap, LightGBM v3) -> `data/predictions.json`
-> `scripts/daily_paper_loop.py` (gap ledger, `execution.gates`, `TicketStore`, settle, `closing_quote` CLV, report).

**No stage had ever scored the SERVED champion.** Rule frozen first (`02_selection_protocol.json`: keep the
champion unless the same-family rebuild wins the price-free line with a paired CI95 excluding zero), then
`03_champion_replay.py` (inference only, dev_oos only, holdout asserted absent):

| line (618 dev races, market 1.70403) | race log loss | gap |
|---|---|---|
| champion v3nf price-free | 2.10003 | -0.39600 |
| champion v3 market-assisted CatBoost | 1.86503 | -0.16100 |
| champion LightGBM v3 | 2.14564 | -0.44161 |
| champion F-L market-adjusted value line | 1.71721 | -0.01318 |
| step-10 rebuild price-free (s10idp) | 1.93620 (reproduces step 10 exactly) | -0.23217 |
| step-10 rebuild market-assisted (s10mkt) | 1.71924 (reproduces step 10 exactly) | -0.01521 |

PRIMARY paired delta (rebuild - champion, price-free): **-0.16383, CI95 [-0.22228, -0.10673]** -> rebuild
selected. Cause measured (`03_feature_skew_psi.csv`): in the champion's own `reference_features.parquet`
`trainer_form_zscore` / `jockey_form_zscore` / `hot_connection_z` have std **0.0000** (D41's dead windows) vs
0.91-0.94 through the repaired pipeline over the same period; `trainer/jockey_hot_strike_rate` PSI 0.43/0.44
(gate threshold 0.2), 16.39% of the champion LightGBM's importance. Served v3nf also still has 40 features
including market-derived `race_complexity` (D40); s10idp has 39 and no price column.

## Scope 3 — frozen manifest (`candidate_manifest.json`, frozen 2026-09-19T13:33:50Z, refuses overwrite)

Model/data/code sha256s; both feature lists; training window dev_core; sigmoid calibration; operating cutoff
(**none deployed** — only the 900 s TTLs); selection threshold read from config (edge 0.02, EV 0.05, field >= 5,
book sum <= 1.25); staking rule (tenth-Kelly, 0.5%/0.5%/3% caps, EUR 50 — **not applied live**); supported market
terms (WIN, named fixed-odds bookmaker; commission 0.0 live; Rule 4 / dead heats / non-runner void / each-way
OFFLINE-ONLY); forward gate retained (8 wk / 200 / 150 / CLV CI > 0 / A/E 0.90-1.10 / ECE 0.03 / DD 10%);
served-bundle hashes as rollback target; 6-step reversible promotion procedure, **not executed**.
Agent-sourced claims in it were each re-verified by grep before the freeze.

## Final holdout — used exactly once, now CONSUMED (`05_final_holdout_once.json`)

Eligibility documented (reserved by D42; verified unscored above). Opened after the manifest freeze; the
script first rehearsed the identical code path on dev_oos (max|delta| = **0** vs the replay) and refuses a
second run. 708/708 races kept, market 1.76165:

| line | log loss | gap | paired CI95 vs market |
|---|---|---|---|
| **s10idp price-free (PRIMARY)** | 2.01029 | -0.24864 | deficit **[+0.20421, +0.29072]** |
| s10mkt market-assisted | 1.76795 | -0.00630 | [-0.00168, +0.01383] |
| champion v3nf / v3 / LightGBM v3 / F-L line | 2.18711 / 1.92570 / 2.18954 / 1.77419 | | |

Candidate does **not** beat the market. Rebuild-beats-champion **replicates**: price-free -0.17682, CI95
[-0.22893, -0.12461]; market-assisted -0.15774, CI95 [-0.21207, -0.10717]. **No untouched holdout remains;
further evidence must be prospective. July 2026 was not relabelled.**

## Scope 4 — bounded paper replay + failure checks (`06_paper_replay.py`, isolated, no network)

All writes under `data/audit/17/replay/<label>/`; real stores sha256+mtime identical before/after.
Real 26-race / 308-runner card: 308 PASS, 0 CANDIDATE, all WIN, all `paper_only`, ids and
`(race_uid, horse_key, market, bet_type)` unique; same-day re-run 0 issued / 308 duplicates; forward gate
fails 9/9, window `not_started`. Fail-closed probes (each PASSes **even under a stub GO verdict**): stale quote,
future-stamped quote, consensus price source, incomplete book, stale/erroring/absent source health,
market-adjusted-only probability, missing/corrupt verdict file, missing predictions file; store refuses a
non-paper ticket. Control proves non-vacuity (clean race + stub GO -> 1 CANDIDATE). **before_fix 24/25, after_fix 30/30.**

`08_settlement_probe.py` (isolated): the identity join reconciles (won/lost/void each to its own result), but
stake is never sized, so a **winner at 3.75 settles profit 0.0 = a loser**; a non-finisher settles `void`;
`settle_ticket` is not called live. `09_live_store_readonly.json` (`mode=ro`): 988 PASS / 0 CANDIDATE /
0 settled / 0 staked, `CHECK (paper_only = 1)` present, 3,547 WIN-only snapshots over **2 capture days**,
0 future stamps, append-only triggers intact. Alerts: only external channel is Telegram, inactive this session;
dedupe suppresses a repeat and re-alerts a new price (isolated store). Runbook: `RUNBOOK.md`.

## Defects FIXED here (both strictly fail-closed)

1. `execution/config.py` — `gates.require_model_validation: false` in YAML issued a CANDIDATE under the real
   NO-GO verdict (F14). Now clamped on at load and reported, like `forward_gate.require_model_go`.
2. `models/predictor.py::_race_ev_gate` — `fetched_at` parsed without `format="mixed"`; under pandas 3 a
   2-hour-stale sub-second row became NaT, `ages.max()` skipped it, race stayed EV-eligible (old logic read it
   as 31 s fresh — reproduced). Now mixed parse + `unreadable_price_timestamp:N` PASS reason.
Tests +3: `tests/execution/test_gates.py` (1), `tests/models/test_predictor.py` (2).

## Open blockers (repro commands in READINESS.md)

B1 HIGH served-bundle train/serve skew · B2 HIGH no live stake sizing, P&L vacuous, drawdown criterion
unreachable · B3 HIGH live settlement bypasses `settle_ticket`; DNF -> void (D37) · B4 PASS rows never settled,
ledger accrues no CLV · B5 one physical race emitted twice (Dundalk 19:30/19:31) + known F4 · B6 no operating
cutoff; stale card re-ticketed next day (308 rows) · B7 no model hash on predictions/tickets · B8 declared
cards not flowing (`timeform.parquet` frozen 2026-06-13; `runner_status` null on 651,154/651,154) · B9 F-L
price mismatch (D44), UI flags 5 "value bets" with gate-EV -0.16..-0.38, 16/26 eligible races over the 1.25
ceiling · B10 lock's odds band unenforced live, drift trigger uncalled, `ui/bet_placer.py` unlabelled.
Corrected agent claims: only **2** (not 20) of 26 races were already off at generation; **16** (not 19) exceed
the overround ceiling. B2-B4 are dormant while 988/988 tickets are PASS.

## Checks actually run (this session)

- `pytest -q` full suite, serial: **2,527 passed, 0 failed**, 1 pre-existing dateutil warning, 140.72 s
  (`pytest_full_suite.txt`). 2,524 at step 16 + 3 new. No pre-existing failures, no regressions.
- Targeted: execution/gates/staking/forward-gate/paper-loop 126 passed; predictor/alerts/value/reconcile 235 passed.
- `01`..`09` scripts all exit 0 (06 before_fix exits 1 by design: it records F14).
- Versions: Python 3.14.7 (.venv), pandas 3.0.3; HEAD 2d7f164 on `chore/config-audit`; dirty entries 192 -> 194
  (`execution/config.py`, `tests/execution/test_gates.py`); nothing staged, stashed, reverted or committed.

## Acceptance

Readiness decision separating collection / formal window / edge, blockers with repro: **met**. Holdout used
at most once with documented eligibility, July not relabelled: **met**. Identity, provenance, settlement
reconciliation, alerts verified; gates retained; real money disabled: **met** (gaps listed, not hidden).
Full suite run, failures classified (none): **met**. Evidence independently verified: **met**.

## Unresolved limits

No promotion executed; B1-B10 open; the step-10 rebuild has only a `won` target; declared-card and live
snapshot paths have 2 capture days of exercise; `config.local.yaml` was not opened, so whether Telegram
credentials exist locally is unknown; `_odds_by_book_map` has the same parse quirk but fails closed (left).

## Next action

Step **18** — start frozen paper collection and hand over daily operation: capture-only/shadow, formal window
NOT started, honour C1-C3, do not change the evaluated model. Recommended model: Codex GPT-6 Astra, thinking
Medium (prompt index); the standing D20 override routes Codex-assigned stages to Claude Sonnet 5.
