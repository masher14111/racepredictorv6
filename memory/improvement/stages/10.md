# 10 — Build comparable baselines and freeze the experiment protocol

Attempt 1: 2026-09-19 (routed to Claude Sonnet 5 per D20) — built the matrix,
froze the manifest, wrote all pipeline scripts and a smoke-test variant, then
the session ended (supervisor kill) while idly waiting on the un-monitored
smoke-test background process. No stage note was ever written; STATE.md
correctly stayed PENDING. Nothing here was fabricated as passing.

Attempt 2: 2026-09-19 (Claude Sonnet 5) — **DONE.** Resumed in place: verified
attempt 1's artifacts rather than discarding them, ran the smoke test to
completion this time, then ran the real frozen-budget pipeline to completion,
added the one missing acceptance item (reload/inference parity for the
conditional-logit and LightGBM families), and ran the full test suite.

Model verdict unchanged: **NO-GO / PAPER-ONLY.** All 6 candidates fail to beat
the market on dev-OOS race-level log loss. This is the first genuinely
price-free baseline line (D40) and it does not change the verdict.

## What attempt 1 already built (verified, not rebuilt)

- `reports/improvement/10/01_build_matrix.py` → `data/audit/10/training_rebuilt.parquet`
  (551,815 rows, 117 cols, 2024-01-01..2026-09-17, through the D26/D27/D33/D38/D39/D40/D41
  pipeline). Re-ran the 5 step-09 regression audits against it this attempt
  (below) instead of trusting attempt 1's unverified build.
- `reports/improvement/10/02_freeze_manifest.py` → `run_manifest.json`, frozen
  2026-09-19T06:33:24Z, BEFORE any candidate was trained. Refuses to freeze if
  the computed windows overlap the already-observed July 2026 window (checked,
  passes). Windows: dev_core 2024-01-01..2026-08-07 (263,982 rows/29,888
  races), dev_oos 2026-08-08..2026-08-27 (5,561 rows/618 races, the
  development scorecard slice), **final_holdout_RESERVED_UNTOUCHED**
  2026-08-28..2026-09-17 (6,754 rows/708 races, never loaded into any fit/CV/
  scoring call — verified by code inspection of `03_run_baselines.py::load_manifest_and_data`,
  which slices `dev_core`/`dev_oos` only and comments why the full frame must
  stay in scope for one parquet read).
- New production modules (required by scope item 2, not present before this
  stage): `models/conditional_logit.py` (race conditional-logit baseline,
  save/load round-trip) and `models/split_utils.py` (whole-race-group
  chronological/walk-forward splitters, D22's named enforcement point).
  Both already had dedicated tests (`tests/models/test_conditional_logit.py`,
  `tests/models/test_split_utils.py`) — 58 targeted tests pass.
- `reports/improvement/10/03_run_baselines.py`: trains market-only (de-vigged
  market, free), conditional-logit, CatBoost and LightGBM grouped-softmax,
  each independent (price-free) + market-assisted, scores all on the SAME
  dev-OOS panel via `models.head_to_head.head_to_head` (identical
  complete-book filter for every candidate and the market line).

## This attempt's work

1. **Regression gate re-run** (step 09's audit scripts, read-only, against
   the rebuilt matrix): all 5 PASS — 0 later-running priors (251,361 trainer /
   265,323 jockey same-day priors, 0.00% not-yet-run), 0 row-order sensitivity
   across 10 windowed-feature paths, day-window units correct (14d window
   averages 18.11 priors vs 79.80 for 90d, not the pre-repair 1:1 collapse),
   market identity/full-field/overround/market_rank invariants hold, 0
   price-free feature reacts to a price perturbation, 0 trainer-collapse
   runner-events lost. Output: `reports/improvement/10/04_audit_*_AFTER_S10.txt`.
2. **Smoke test** (`_smoke_run_baselines.py`, 1 trial/2-fold): ran to actual
   completion this time (980s) — all 6 candidates trained, scored, scorecard
   + predictions written, exit 0. Validated the pipeline end-to-end before
   committing to the full budget. Archived under `_smoke_artifacts/`.
3. **Full frozen-budget run** (6 trials, 5-fold walk-forward CV, matching
   `run_manifest.json`'s declared `tuning_budget`): 2,704s, exit 0, no
   tracebacks. Wrote `reports/improvement/10/scorecard.json` and
   `data/audit/10/dev_oos_predictions.parquet` (5,561 rows: race/horse ids,
   outcome, bet/close price, `condlogit_{indep,market}_prob`,
   `catboost_{indep,market}_prob`; LightGBM's per-branch probabilities live in
   its own `models_lgbm_{indep,market}/holdout_dev_oos/ledger.csv`, not merged
   into the shared parquet — a real gap for step 12+'s later blend work,
   recorded here rather than silently assumed present).
4. **Reload/inference parity** (`05_reload_parity.py`, new this attempt —
   scope item 5 was otherwise unmet): CatBoost already reloads from disk via
   `backtest.holdout.load_frozen_model` inside the main run. Conditional logit
   and LightGBM scored dev-OOS from the in-memory object during training, so
   this script independently reloads each saved artifact
   (`models_condlogit/*.json`, `models_lgbm_*/lgbm_won.txt`) and recomputes
   its dev-OOS probabilities / head-to-head log loss. All 4 checks PASS,
   exact match (max|delta|=0). Output: `05_reload_parity.txt`.
5. **Full test suite**: 2,378 passed (up from step 09's 2,365 — the 13 new
   conditional-logit/split-utils tests), serial, 136.49s, exit 0. No
   regression.

## Scorecard (dev-OOS, 618 races, 5,561 runners; race-level log loss)

| candidate | model LL | market LL | gap | beats market |
|---|---|---|---|---|
| market-only | 1.70403 | — | — | (baseline) |
| condlogit_indep | 1.92141 | 1.70403 | -0.2174 | **No** |
| condlogit_market | 1.70729 | 1.70403 | -0.0033 | **No** |
| catboost_indep | 1.93620 | 1.70403 | -0.2322 | **No** |
| catboost_market | 1.71924 | 1.70403 | -0.0152 | **No** |
| lgbm_indep | 1.91176 | 1.70403 | -0.2077 | **No** |
| lgbm_market | 1.70664 | 1.70403 | -0.0026 | **No** |

Every price-free (independent) branch is far worse than the market (~0.21-0.23
log loss gap) — an honest, expected result, not a bug. Every market-assisted
branch is close but still slightly worse (gap 0.0026-0.0152). No candidate
beats the market; the historical NO-GO verdict is reconfirmed on the first
genuinely price-free line, not overturned.

**Weighting ablation** (market-assisted conditional logit, dev_core walk-forward
CV log loss, l2 fixed at the market branch's tuned value): existing
odds-inverse sample weight **1.86134**, unweighted **1.72132**, race-weighted
**1.72253**. The existing weighting scheme is materially worse in-fold than
either alternative on this model family — a genuine, disclosed finding, not
adopted here (out of this stage's scope to change production weighting; a
named candidate for whichever step next revisits CatBoost/LightGBM sample
weights).

## Acceptance

- Reproducible baseline scorecard + out-of-sample dev predictions share one
  eligible-race/cutoff protocol (`run_manifest.json`), frozen before training. **Met.**
- Final-test policy frozen; `03_run_baselines.py` never selects those rows
  into any fit/CV/scoring call (verified by code path, not just by claim). **Met.**
- Champion artifacts preserved: `git status` shows no modification to
  `models/*.bin`/`models/lgbm_won_v3.txt`/`models/lgbm_v3_meta.json`;
  candidates live only under `data/audit/10/models_*`. **Met.**
- Performance reported honestly as NO-GO across all 6 candidates; no
  threshold manipulation. **Met.**
- Reload/inference parity verified for all 3 model families. **Met (this attempt).**

## Unresolved limits (deliberately not fixed here)

- LightGBM's dev-OOS probabilities are not merged into the shared
  `dev_oos_predictions.parquet` (see item 3 above) — a follow-up must read
  its per-branch ledger CSVs instead.
- Liquidity/matched-volume data remains absent (`backtest.holdout` integrity
  WARN on every LightGBM run) — real execution cost on outsiders still can't
  be verified; unchanged from steps 07/08's named gap.
- All step 08/09 deferred items stand unchanged (snapshot venue-collision on
  odds_finish parse, live settlement not calling `execution.settlement`,
  F8's row-count-based `_feature_selection.py`, not re-run).
- Weighting ablation is a development-fold finding only; not applied to any
  production model in this stage.

## Files

- `reports/improvement/10/{01_build_matrix,02_freeze_manifest,03_run_baselines,05_reload_parity}.py`,
  `04_audit_*.py` (step 09's read-only regression scripts, rerun here).
- `reports/improvement/10/run_manifest.json`, `scorecard.json`,
  `coverage_training_rebuilt.json`, `05_reload_parity.txt`,
  `04_audit_*_AFTER_S10.txt`, `_full_run.log`, `_smoke_artifacts/`.
- `data/audit/10/training_rebuilt.parquet`, `dev_oos_predictions.parquet`,
  `models_condlogit/`, `models_catboost/`, `models_lgbm_{indep,market}/`.
- New production: `models/conditional_logit.py`, `models/split_utils.py`.

## Versions

Python via `.venv/Scripts/python.exe`; CatBoost `task_type=GPU` (RTX 5070 Ti);
LightGBM CPU, `env=py3.14-native`. Full suite: 2,378 passed, 1 pre-existing
unrelated warning (timezone parsing), 136.49s serial.

## Next action

Step 11 (measured speed/sectionals pilot), prerequisite 10 now satisfied.
Recommended model: Codex GPT-6 Astra per the prompt index; standing D20
override routes Codex-assigned stages to Claude Sonnet 5 until lifted.
Thinking level: High.
