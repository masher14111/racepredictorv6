# 01 — Checkpoint the project and agree data/evaluation contracts

Date: 2026-09-19 (supervisor-resumed attempt; prior 2026-09-18T23:50:50Z session recorded
`status: running` in `logs/improvement-runner/status.json` with no completion and left no
stage evidence file — treated here as never-finished, redone from scratch, not trusted).
Status: DONE (audit/contract stage; no model training or acceptance claimed).

## Objective
Checkpoint current Git/environment/data state and produce one shared identity/timing/
evaluation contract (`docs/improvement/CONTRACTS.md`) for steps 02..18, per
`docs/improvement/prompts/01-checkpoint-and-contracts.md`.

## What was done
1. Baseline manifest saved under `reports/improvement/01/`:
   - `git_status_20260918T2351Z.txt` (115 changed/untracked entries, branch
     `chore/config-audit` at `2d7f164`; unrelated dirty tree preserved, nothing reset/staged).
   - `pip_freeze_20260918T2351Z.txt` (venv `.venv/Scripts/python.exe`, Python 3.14.7; key
     packages: catboost 1.2.10, lightgbm 4.7.0, numpy 2.4.6, pandas 3.0.3, scikit-learn 1.9.0,
     streamlit 1.58.0).
   - `audit_reproduction_20260918T2351Z.txt` (raw command output for all checks below).
2. Reproduced dated findings on current `data/features/training.parquet` (mtime
   2026-07-27T09:28:22 local, not regenerated this stage):
   - 260,490 rows; 148,860 WIN / 111,630 PLACE; dates 2024-01-01..2026-07-25. Matches STATE.md.
   - Fully-null columns confirmed at 100%: timeform_rating, rating_rank, pace_bias, race_class,
     class_change, recent_form_avg. going_speed filled 20.92%. Matches STATE.md.
   - `data/spotlight.parquet`: 243 rows, all dated 2026-06-17. Matches STATE.md.
   - `reports/forward_validation_20260918.md` (existing saved report, not re-run): independent
     LL 1.8740 vs market LL 1.6878 over 904 races; 988 open paper tickets, 0 settled. Matches
     STATE.md.
   - **New this stage**: found that `race_id` in the training matrix is entirely null
     (`nunique()==0`); the prior dated "shared races" figures (train/test 28, fit/calibration
     13, fit/early-stop 45) only reproduce when computed on `race_uid` (the real per-race
     identity, 29,512 unique values), not `race_id`. Using `race_id` alone silently reports
     zero overlap because both sides only ever contain `None`. This is a live discrepancy risk
     for any later stage that reads STATE.md's numbers without checking which column they used.
   - Root cause located: `models/train.py::_time_split` (line 94) sorts by `race_date`
     (date-only) then cuts by absolute row count — no per-race or per-day boundary — so races
     whose rows straddle the cut index leak across train/test, fit/calibration and
     fit/early-stop. Confirmed reproducible with configured `test_size=0.2`,
     `calibration_size=0.15`, early-stop fraction 0.10 (values read from `config.yaml`, not
     assumed). Fixing this is explicitly step 02's scope, not this stage's.
   - Confirmed no explicit "minutes-before-off" prediction cutoff constant exists anywhere in
     `config.yaml`/scraper/model code; documented a provisional 10-minute research-only
     assumption in CONTRACTS.md, matching the existing `race_soon_minutes: 10` alert value.
     This does not change any live/service behavior.
3. Wrote `docs/improvement/CONTRACTS.md` (114 lines): race/horse/market identity (`race_uid`
   canonical, `race_id` dead), timezone/as-of semantics, independent-vs-market feature caveats
   (`race_complexity`, `horse_speed`), missingness table, candidate artifact ownership rules,
   the complete-race-splitting defect above, final-test policy (July 2026 window already
   observed, must not be reused untouched), and named data gaps.
4. Data rights/access: `OPENROUTER_API_KEY` confirmed absent from this shell's environment
   (checked with a presence test only, never printed or searched for); `config.local.yaml`
   confirmed to exist but was not opened or printed. Hosted DeepSeek trial remains
   `DEFERRED_DATA` — unchanged from HANDOFF.md.
5. Bounded offline sanity check: `.venv/Scripts/python.exe -m pytest -q
   tests/utils/test_bet_settlement.py tests/utils/test_proxy_manager.py` → 89 passed in 5.19s.
   Chosen as a small, fast, already-existing pair of suites relevant to settlement/provenance
   claims used above; not a full-suite run (reserved for later integration milestones per
   AGENTS.md).

## Files touched
- Added: `docs/improvement/CONTRACTS.md`, `reports/improvement/01/*` (3 files),
  `memory/improvement/stages/01.md` (this file).
- Updated: `memory/improvement/STATE.md`, `memory/improvement/HANDOFF.md`.
- No application/model/config code was changed. No unrelated dirty file was touched, reset,
  or staged.

## Acceptance check
- [x] Dirty-tree and environment fingerprints, dated measurements and commands saved under
      `reports/improvement/01/`; no unrequested application changes made.
- [x] One shared contract (`CONTRACTS.md`) resolves identity/timing terminology for later
      prompts, in particular naming `race_uid` as canonical and `race_id` as dead.
- [x] Memory lists genuinely verified facts (all reproduced with real commands this session),
      named external-data gaps (text corpus single-day, no timing/odds-history corpus, hosted
      key absent), and the exact next step (02).

## Limitations / unresolved
- Horse-identity-within-race and cross-race horse-history join keys were not audited; deferred
  to whichever stage first needs them (not blocking step 02).
- `race_complexity`'s market-derived-ness and `horse_speed`'s outcome-adjacency are carried
  over from the prior dated review, not independently re-derived from source code in this
  stage; flagged in CONTRACTS.md as needing re-verification before being relied on.
- No new odds/timing "as-of" capture corpus was found or created; step 07 owns that gap.

## Next action
Step 02 — Whole-race chronological splits (`docs/improvement/prompts/02-whole-race-splits.md`).
Fix `_time_split` (and the calibration/early-stop carve in `models/train.py`) to split on
`race_uid`/day boundaries, not absolute row count, and re-verify the 28/13/45 overlap drops to
zero. Recommended model: Codex — GPT-6 Astra, Medium (fallback: Claude Sonnet 5 if Codex limit
still applies).
